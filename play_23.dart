void main() {
  // List
  // ignore: unused_local_variable
  List names = ['Jack', 'Jill'];
  // print(names.length);

  var name_s = ['Jack', 'Jill'];
  // print(name_s.length);
  for (var n in name_s) {
    print(n);
  }
}
